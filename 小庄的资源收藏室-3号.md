# [装机必备]Geek（简洁的卸载工具，强力卸载，卸载后清理）
https://geekuninstaller.com/download
----
> [UninstallTool-批量卸载](https://zjazn.lanzoub.com/iFJft2vp9c5a)
> [RevoUninstaller-Pro-批量卸载/猎人模式](https://zjazn.lanzoub.com/iiWOh30cbikf)


# 小/中/高/大学教材（“虽然国内教育网站已提供免费资源，但大多数普通人获取信息的途径依然受限。有些人利用这一点，在淘宝上销售这些带有私人水印的资源。为了应对这种情况，我计划将这些资源集中并开源，以促进义务教育的普及和消除地区间的教育贫困。”）
https://github.com/TapXWorld/ChinaTextbook

# EVC视频录制（一款不错的录屏软件，有会员机制）
https://www.ieway.cn/evcapture.html
----
其它：
- [土豆录屏（Windows免费无广告软件，界面简洁易用，可设置快捷键开始/结束录屏）](http://tudouluping.com/?client=windows)


# 小风车（Win电脑动态壁纸，将MP4视频文件放在指定的目录下 软件所在目录/wallpaper 下重启即可在软件中选择该动态壁纸）
https://github.com/Izumiko/xiaofengche

# UP云搜 (各网盘资源搜索，最重要的是有蓝奏云，资源新)
https://www.upyunso.com/[[search.html?keyword={keyword}&pan_channel=lanzou]]

# JexChan（单文件制作工具，注意选好入口exe文件，其它不怎么需要配置了，制作默认放在选择的项目同级下）
https://www.52pojie.cn/thread-1773619-1-1.html


# 宝塔（“宝塔Linux面板是提升运维效率的服务器管理软件，支持一键LAMP/LNMP/集群/监控/网站/FTP/数据库/JAVA等100多项服务器管理功能。”，总之界面化的运维工具，部署前需要干净的系统）
https://www.bt.cn/new/product_linux.html
----
> [国际版：aapanel](https://www.aapanel.com/)

## 使用宝塔部署一个SpringBoot+Vue项目
1、安装宝塔
一键脚本，兼容多发行版linux环境安装宝塔
```bash
if [ -f /usr/bin/curl ];then curl -sSO https://download.bt.cn/install/install_panel.sh;else wget -O install_panel.sh https://download.bt.cn/install/install_panel.sh;fi;bash install_panel.sh ed8484bec
```
安装完后会输出登录的`用户名`/`密码`
2、安装环境
进入界面时，选择java环境安装；
如果系统依赖Redis进去应用商店安装Redis，那在左侧数据库就有Redis了

3、部署后端
在本地电脑将后端配置好生产环境将项目打包为`jar`包，上传到 `/opt/java-project/backend`下面
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/07/07/1751853068675.png)

ps: 后端统一接口路径为`/api`

4、部署前端静态项目
将前端打包为dist，上传到 `/opt/java-project/frontennd/dist`;

创建一个html的项目，需要注意的是这里一定要填入域名，所以提前要做的是添加域名的DNS解析到该台机器。

添加 `/api转后端配置` 、 `兼容vue单页面访问配置（所有直接都/index.html来作为入口，不配置刷新就404了，因为只有index.html）`。请看下面html配置文件的配置中的注释。

下面`html项目`-> `配置文件`: 
```conf
server {
    listen 80;
    listen 443 ssl;
    listen 443 quic;
    http2 on;

    server_name easypan.spark.publicvm.com;
    root /opt/easypan/frontend/dist;
    index index.html index.htm default.htm default.html;

    # CERT-APPLY-CHECK--START
    # 用于SSL证书申请时的文件验证相关配置 -- 请勿删除并保持这段设置在优先级高的位置
    include /www/server/panel/vhost/nginx/well-known/easypan.spark.publicvm.com.conf;
    # CERT-APPLY-CHECK--END

    # SSL-START SSL相关配置，请勿删除或修改下一行带注释的404规则
    # error_page 404/404.html;

    # HTTP_TO_HTTPS_START
    set $isRedcert 1;
    if ($server_port != 443) {
        set $isRedcert 2;
    }
    if ($uri ~ /\.well-known/) {
        set $isRedcert 1;
    }
    if ($isRedcert != 1) {
        rewrite ^(/.*)$ https://$host$1 permanent;
    }
    # HTTP_TO_HTTPS_END

    ssl_certificate     /www/server/panel/vhost/cert/easypan.spark.publicvm.com/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/easypan.spark.publicvm.com/privkey.pem;
    ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_tickets on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000";
    add_header Alt-Svc 'quic=":443"; h3=":443"; h3-29=":443"; h3-27=":443"; h3-25=":443"; h3-T050=":443"; h3-Q050=":443"; h3-Q049=":443"; h3-Q048=":443"; h3-Q046=":443"; h3-Q043=":443"';

    error_page 497 https://$host$request_uri;
    # SSL-END

    # ERROR-PAGE-START 错误页配置，可以注释、删除或修改
    # error_page 404 /404.html;
    # error_page 502 /502.html;
    # ERROR-PAGE-END

    # REWRITE-START URL重写规则引用,修改后将导致面板设置的伪静态规则失效
    include /www/server/panel/vhost/rewrite/html_easypan.spark.publicvm.com.conf;
    # REWRITE-END

    # 禁止访问的文件或目录
    location ~ ^/(\.user.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README.md) {
        return 404;
    }

    # 一键申请SSL证书验证目录相关设置
    location ~ \.well-known {
        allow all;
    }

    # 禁止在证书验证目录放入敏感文件
    if ($uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$") {
        return 403;
    }

    # 静态资源缓存
    location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$ {
        expires 30d;
        error_log /dev/null;
        access_log /dev/null;
    }

    location ~ .*\.(js|css)?$ {
        expires 12h;
        error_log /dev/null;
        access_log /dev/null;
    }

    # 接口代理配置，转发到后端
    location /api/ {
        # 如果后端不希望看到 /api 前缀，则解开下行注释
        # rewrite ^/api/(.*)$ /$1 break;

        proxy_pass http://127.0.0.1:7090/api/;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        # 显式设置 Authorization 请求头
        proxy_set_header Authorization $http_authorization;
    }

    # 前端路由支持（单页应用 history 模式）
    location / {
        try_files $uri $uri/ /index.html;
    }

    access_log /www/wwwlogs/easypan.spark.publicvm.com.log;
    error_log  /www/wwwlogs/easypan.spark.publicvm.com.error.log;
}
```

5、配置SSL
在html项目的的SSL配置中，通过文件验证的方式：
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/07/07/1751853578131.png)

ps: 部署好SSL后推荐开启强制https

一键命令：
```bash
#示例命令，请替换为自己的值！！！！！
echo "pWPhb6l1oYpACgeoWmXmZw2ZrtT7PIqfsDIcfjLc-e0" > /opt/easypan/frontend/dist/.well-known/acme-challenge/nkWKWdXKqEbO2XtaTAfUHFzUcpgqxzJsQBE8DqvPgqw
```

最后效果：
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/07/07/1751853830360.png)

# SpringBoot（文档 & 相关）
https://springdoc.cn/spring-boot
----
> [DockerComposeForSpringBootVue](https://gitee.com/zhuangjieya/docker-compose-for-springboot-vue)


# Telepresence(K8S环境与本地联调工具)
https://telepresence.io
----
> [下载](https://github.com/telepresenceio/telepresence/releases)

## 1、本地建立与K8S的连接（与Telepresence无关）
```cmd
#更多：https://kubernetes.io/releases/download/#binaries
curl.exe -LO "https://dl.k8s.io/release/v1.33.0/bin/windows/amd64/kubectl.exe"
```
将`kubectl.exe`所在目录放在path环境变量. 
将k8s连接信息放在指定目录，让kubectl.exe可以识别上. 将k8s-master节点的`$HOME/.kube/config` , 将值放在本地`C:\Users\zhuangjie\.kube\config`。
```cmd
kubectl get node
```
如果显示节点信息就表示本地连接上了远程的k8s，可以使用本地kubectl操作k8s。

## 2、使用telepresence
[下载](https://github.com/telepresenceio/telepresence/releases) ，windows一般下载amd架构的。
安装：不同版本安装不一样，在`2.23.3`版本需要用powershell好执行install脚本来安装。

`telepresence helm install`， 会向k8s中安装`traffic-manager`, 安装在命名空间 `ambassador`下.

在IDEA安装`kubernetes`插件，进行下面的配置，如果用到telepresence也会识别本地是否安装telepresence。
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/07/15/1752561797193.png)

先让telepresence connect：
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/07/15/1752563299996.png)
然后就可以拦截depoloy的指定端口了,填入`localPort:remotePort/name` 与telepresence intercept支持的参数：
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/07/15/1752563367683.png)

> 如果匹配端口有两个service，那可能还要指定要拦截哪个service了。
> `telepresence intercept my-deployment --port 8080:http --service my-service-a`

注意：`telepresence intercept my-deployment --port 8080:http` 8080是本地要对接的服务，而http是拦截的端口的名称，为什么不直接写端口，这是因为如果你k8s如果版本小于`1.29`版本，此时pod中没有iptable，使用http这种的话就可以避免 Telepresence 注入init-container来插入iptables规则。不知道是否有说错，可以[具体参考官网](https://telepresence.io/docs/troubleshooting#injected-init-container-doesnt-function-properly) 。所以如果不这样做可能就无法拦截，拦截时命令执行失败！
```yml
apiVersion: v1
kind: Pod
metadata:
  ...
spec:
  containers:
    - ports:
      - name: http
        containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  ...
spec:
  ports:
    - port: 80
      targetPort: http
```


telepresence主要是拦截指定namespace->deploy->容器 的端口的请求拦截转发到本地服务下，配置继续看下面
如果是微服务，在暴露服务地址时，也要配置，以nacos为例：
```yml
spring:
  cloud:
    nacos:
      discovery:
        # 开发环境需要（生产也可用），不直接调用pod，而是交给k8s service进行请求路由
        ip: spark-coupon.spark-test.svc.cluster.local
```
> 如果不配置上面，由于微服务注册的地址是pod的地址，而telepresence又拦截不了pod的地址，所以要配置，如果后面可以了，猜测可能跟k8s集群版本与telepresence都要支持有关。



# 彩虹易支付（“彩虹易支付系统，专注于聚合支付网站解决方案提供,以信誉求市场，以稳定求发展，行业内最安全，简单易用，专业的技术团队，最放心的聚合支付系统。”）
https://github.com/lopinx/epay
----
> [docker-compose部署](https://onedrive.live.com/?redeem=aHR0cHM6Ly8xZHJ2Lm1zL2YvYy9iZTRhMDllMDY3NjM4ZWE5L0VzYk9wWVJydWVoTXYzNDJfN2JfR1ZvQnZad20yX1l4ZE11TFp6ZG12YjlOU1E%5FZT1WNUFMTlU&id=BE4A09E067638EA9%21s84a5cec6b96b4ce8bf7e36ffb6ff195a&cid=BE4A09E067638EA9)

# kkFileView (文件预览服务，可以在部署自己项目时也部署一下这样的预览服务，在要进行文件预览时调用该服务进行文件预览，支持docker镜像部署)
https://github.com/kekingcn/kkFileView
----
> [如何使用？](https://kkview.cn/zh-cn/docs/production.html)
> [在线体验预览服务](https://file.kkview.cn/)

# NginxUI(“Nginx UI 是一个全新的 Nginx 网络管理界面，旨在简化 Nginx 单机和集群节点的管理和配置。 它提供实时服务器运行数据、Nginx 性能监控、ChatGPT 助手、支持大模型代码补全的代码编辑器、 一键部署 Let's Encrypt 证书的自动续签以及用户友好的网站配置编辑工具。此外，Nginx UI 还提供了在线访问 Nginx 日志、配置文件的自动测试和重载、网络终端、深色模式和自适应网页设计等功能。 Nginx UI 采用 Go 和 Vue 构建，确保在管理 Nginx 服务器时提供无缝高效的体验。”)
https://nginxui.com/zh_CN/
----
使用示例：
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/08/06/1754464648708.png)
首先想要用nginUI来替代自己手动修改Nginx配置文件的方式，需要了解NginxUI是如何管理Nginx配置的，如果你是将所有的网站代理放在`nginx.conf`这个nginx配置文件里就不符合nginxUI的管理规范，那nginxUI是如何工作的呢？
```md
nginx/
- ngin.conf : 配置的入口文件，其它子配置会从这里includes, 默认include下面两个：
      include /etc/nginx/conf.d/*.conf;
      include /etc/nginx/sites-enabled/*;
- conf.d/ ：nginx自己的代理访问配置放在这里，如果没有这个配置，就无法访问NginUI的管理面板
- sites-available/ ：在nginxUI创建的“网站”是以一个文件放在这里的，如果我们“开启”这个网站后，那会创建一个软链接放在`sites-enabled/`下面，那这个网站配置就生效了。
- sites-enabled/ : 存放生效/开启的的网站配置，详细请看`sites-available/`

...其它配置文件
```

1、docke-compoe部署
> 如果你当前的有服务占用着80与443端口，你可以将下面修改为其它端口，等迁移到nginxUI后再停止原来的nginx，再将端口改回为80与443。
```bash
cat << EOF > docker-compose.yml

services:
  nginx-ui:
    image: uozi/nginx-ui:latest
    container_name: nginx-ui
    restart: always
    environment:
      - TZ=Asia/Shanghai
    volumes:
      # 当前目录下的 nginx 文件夹映射到容器内的 /etc/nginx
      - ./nginx:/etc/nginx
      - ./ssl:/etc/ssl
      # 当前目录下的 nginx-ui 文件夹映射到容器内的 /etc/nginx-ui
      - ./nginx-ui:/etc/nginx-ui
      # 当前目录下的 www 文件夹映射到容器内的 /var/www
      - ./www:/var/www
      # 保持 Docker 套接字映射（用于容器管理功能）
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "80:80"
      - "443:443"
    
EOF
docker-compose up -d

docker-compose down #关闭进行修改上面初始出来的配置
```
2、手动初始化

你需要将你用到的证书放到与`docker-compose.yml`同级的ssl目录下。然后进入`./nginx/conf.d/nginx-ui.conf` 进入配置nginxUI管理面板的https, 然后再启动nginxUI, 使用https来访问nginxUI项目。

4、创建网站
点击nginxUI web面板的左侧“网站管理”->“添加站点”，此时只需要填`名称`与`server_name`即可，然后保存，后面再使用`高级模式`编辑即可。
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/08/06/1754463803254.png)


# CurrPorts（#杀端口#端口被占用 “CurrPorts 是一款 Windows 平台下的轻量级网络连接查看工具，可实时显示所有进程占用的端口及对应的进程名、路径、远程地址等信息，支持筛选和导出。”）
https://www.nirsoft.net/utils/cports.html
----
> [下载](https://www.nirsoft.net/utils/cports-x64.zip)

想知道指定的端口是否被占用：根据`Local Port` 排序（点列头）查看；
想知道哪些服务占用了哪些端口：根据`Process Name`排序（点列头）查看；
![](https://cdn.jsdelivr.net/gh/18476305640/typora@master/images/2025/08/07/1754556532016.png)

# Gitee(“Gitee（码云）是开源中国于2013年推出的基于Git的代码托管和协作开发平台，提供中华人民共和国本土化的代码托管服务”)
https://so.gitee.com[[/?q={keyword}&sort_by_f=Star%20%E6%95%B0]]

# ManusAI（“ManusAI 是一个全能的通用人工智能代理，能够自主规划、调用工具并安全地执行从编程、研究到创意设计等任何复杂的数字化任务”）
https://manus.im/

# 小旺AI截图（“轻量的AI截图工具，截图后进行AI提取文字、AI翻译、AI解释、长截图-手动滚动”）
https://www.xiaowang.com/
----
其它
- TTime（一款简洁、高效、高颜值的输入、截图、划词翻译软件）：https://ttime.timerecord.cn/


# Autolt（Windows系统，通过编写Autoit的脚本可以实现一些自动化，内置打包体积小很方便）
https://www.autoitscript.com/
----
> [au3文档](https://www.jb51.net/shouce/autoit3/index.html)

## 基本使用教程
1、安装: [下载](https://www.autoitscript.com/cgi-bin/getfile.pl?autoit3/autoit-v3-setup.zip)
2、打开安装后附带安装的编辑器`SciTE Script Editor`，但都没有颜色感觉，将写好的脚本写好，保存。
```au3
; hello.au3

; 弹出消息框显示 Hello World
MsgBox(0, "提示", "Hello World")

```
3、用资源文件管理器打开保存的文件所在目录后，直接点击可执行脚本，也可以右键选择 `Compile Script(X64)`构建为.exe文件放在其它电脑上可直接点击运行了。


## 语法收藏
; 获取当前脚本所在的目录
Local $filePath = @ScriptDir & "\volta-2.0.2-windows-x86_64.msi"
; 点击窗口的元素（附带的`Autolt v3 Windows Info` 工具可以抓窗口的信息，下面的`&Next`就是 点`Finer Tool`后下面Visible Text 的行文本）
ControlClick("Volta Setup", "", "&Next")
; 等待安装窗口激活
WinWaitActive("Volta Setup", "")
; 删除文件
FileDelete($savePath)
; 文件夹拷贝
#include <FileConstants.au3>
Local $jetbraPath = @ScriptDir & "\jetbra"
If DirCopy($jetbraPath, "C:\Program Files\jetbra", $FC_OVERWRITE + $FC_CREATEPATH) = 0 Then
    MsgBox(16, "错误", "复制 jetbra 文件夹失败，请检查路径和权限。")
EndIf
; 单文件拷贝
#include <FileConstants.au3>
Local $src = @ScriptDir & "\node\.npmrc"
Local $dst = EnvGet("USERPROFILE") & "\.npmrc"
If FileExists($dst) Then
    FileDelete($dst)
EndIf
If FileCopy($src, $dst, 1) = 0 Then
    MsgBox(16, "错误", "拷贝 .npmrc 失败，请检查路径和权限。")
EndIf
; 使用curl下载文件
RunWait('curl -L -o "' & $savePath & '" "' & $url & '"', "", @SW_HIDE)
; 检查文件是否存在
#include <FileConstants.au3>
If Not FileExists($nodeInstallExePath) Then
    MsgBox(16, "错误", "未找到安装程序：" & @CRLF & $nodeInstallExePath)
    Exit
EndIf
; 执行exe文件
#include <FileConstants.au3>
Local $nodeInstallExePath = @ScriptDir & "\node\install.exe"
Local $result = RunWait('"' & $nodeInstallExePath & '"', @ScriptDir & "\node", @SW_SHOW)
; ===================环境变量配置：开始=====================
; 以配置java环境变量为例
; 注册表路径
Local $regenv = "HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager\Environment"

; 获取脚本所在目录，作为 JDK 根目录
Local $jdkPath = @ScriptDir

; 写 JAVA_HOME 和 CLASSPATH
RegWrite($regenv, "JAVA_HOME", "REG_EXPAND_SZ", $jdkPath)
RegWrite($regenv, "CLASSPATH", "REG_EXPAND_SZ", ".;%JAVA_HOME%\lib\dt.jar;%JAVA_HOME%\lib\tools.jar")

; 读取原来的 Path
Local $oldPath = RegRead($regenv, "Path")
If @error Then
    MsgBox(16, "错误", "读取 Path 环境变量失败")
    Exit
EndIf

; 要追加的路径
Local $newPathEntry = "%JAVA_HOME%\bin"

; 如果 Path 中没有这个路径，则追加
If StringInStr($oldPath, $newPathEntry) = 0 Then
    Local $newPath = $oldPath & ";" & $newPathEntry
    RegWrite($regenv, "Path", "REG_EXPAND_SZ", $newPath)
    MsgBox(64, "成功", "Path 已追加新的路径")
Else
    MsgBox(64, "提示", "Path 已经包含该路径，无需追加")
EndIf


; 刷新系统环境变量
Local Const $HWND_BROADCAST = 0xFFFF
Local Const $WM_SETTINGCHANGE = 0x001A

DllCall("user32.dll", "int", "SendMessageTimeout", _
    "hwnd", $HWND_BROADCAST, _
    "uint", $WM_SETTINGCHANGE, _
    "wparam", 0, _
    "str", "Environment", _
    "uint", 0, _
    "uint", 1000, _
    "uint*", 0)

MsgBox(64, "完成", "环境变量已配置并刷新，请重新打开命令行窗口以生效。")

; ====================环境变量配置：结束====================
; 定义与使用函数库
dir
 - lib
  - CmdHelper.au3
    ```au3
    #include <FileConstants.au3>
    #include <MsgBoxConstants.au3>

    Func RunCmdArray(ByRef $aCommands, $bAdmin = False, $bShowWindow = False)
        If Not IsArray($aCommands) Then
            MsgBox($MB_ICONERROR, "错误", "传入参数不是数组")
            Return False
        EndIf

        Local $sTmpBat = @ScriptDir & "\tmp_cmd.bat"

        ; 写入批处理文件，强制使用 ANSI（GBK）避免中文乱码
        Local $hFile = FileOpen($sTmpBat, $FO_OVERWRITE + $FO_CREATEPATH)
        If $hFile = -1 Then
            MsgBox($MB_ICONERROR, "错误", "无法创建临时文件: " & $sTmpBat)
            Return False
        EndIf

        FileWriteLine($hFile, "@echo off")
        ; 设置 cmd 默认代码页为 65001 (UTF-8) 如果希望用 UTF-8
        ; FileWriteLine($hFile, "chcp 65001 >nul")
        For $i = 0 To UBound($aCommands) - 1
            FileWriteLine($hFile, $aCommands[$i])
        Next
        FileClose($hFile)

        ; 执行命令
        Local $iSW = $bShowWindow ? @SW_SHOW : @SW_HIDE
        If $bAdmin Then
            ; 管理员权限执行（异步）
            ShellExecute($sTmpBat, "", @ScriptDir, "runas")
            Sleep(3000) ; 可根据实际调整等待 UAC 弹出
        Else
            ; 普通权限，RunWait 会等待执行完
            RunWait(@ComSpec & " /c " & '"' & $sTmpBat & '"', @ScriptDir, $iSW)
        EndIf

        FileDelete($sTmpBat)
        Return True
    EndFunc
    ```
 - test.au3
    ```au3
    #include "lib\CmdHelper.au3"

    ; 定义命令数组，每个元素是一行 CMD 命令
    Local $aCmds[3]
    $aCmds[0] = "echo hello123"
    $aCmds[1] = "timeout /t 10 /nobreak"
    $aCmds[2] = "echo 10秒等待结束"

    ; 普通权限执行并等待完成
    RunCmdArray($aCmds, False, True)

    ; 管理员权限执行示例
    ; Local $aAdminCmds[1] = ["mkdir C:\TestFolder"]
    ; RunCmdArray($aAdminCmds, True, True)

    ```

# 动漫共和国（看4K动漫，更新快，速度快，讨论，视频仅会员下载）
https://www.oneghg.com

# 3DM(下载一些软件挺靠谱的，挺多软件都有更新时间也比较及时，除普通软件还有游戏、硬件一些模块)
https://soft.3dmgame.com

# Dism++ (系统优化，驱动备份/安装)
https://github.com/Chuyu-Team/Dism-Multi-language
----
> [ZyperWinOptimize](https://github.com/ZyperWave/ZyperWinOptimize "一键进行不同程度的优化：基本/深度/极限优化，还能进行office安装、windows系统激活、垃圾清理")
